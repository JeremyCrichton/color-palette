import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    width: '20%',
    height: '25%',
    display: 'inline-block',
    position: 'relative',
    marginBottom: '-3.5px'
  }
});
